<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>for반복문 연습2</title>
<link rel="stylesheet" href="../css/my.css">
<script>
//누적된 값 더하기 
proc1 = () => {
	sum = 0;
	sustr = "";
	while (true) {
		//입력 vinput
		vinput = parseInt(prompt("더하려는 숫자를 입력하세요."))
		//입력값이 0 이면 break
		if (vinput == 0) break;

		//아니면 입력값을 더한다. (초기값에 따라 결과가 달라짐)
		sum += vinput;
		sustr += vinput + ",";
	}
	//출력내용
	str = "입력된 값들 : " + sustr + " <br>"
	str += "입력된 값을 숫자(덧셈)로 출력: " + sum;
	//출력
	document.getElementById('result1').innerHTML = str;
}

// 3의 배수일 때만 더하기 

proc2 = () => {
	iksu = "";
	for (var i = 1; i <= 10; i++) {
		for (var k = 1; k <= 10; k++) {
			if ((i+k) % 3 != 0) continue;
			iksu += "( " + i + ", " + k + " )"+ "&nbsp;&nbsp;&nbsp;결과 : "+ (i+k) +"<br>";
		}
	}
	//출력
	document.getElementById('result2').innerHTML = iksu;
}

//2의 배수 & 3의 배수인 수를 출력 (1~100)
proc3 = () => {
	susu = "";
	for (var i = 1; i <= 100; i++) {
		if (i%2 == 0 && i%3 == 0) {
			susu += "[" + i + "] ";
		}
	}
	
	//출력 
	document.getElementById('result3').innerHTML = susu;
}

// 두 수 입력하여 100 넘으면 출력 (두 수 모두 0입력 되면 종료)
proc4 = () => {
	// 입력 - su1, su2
	su1 = parseInt(prompt("첫 번째 수를 입력하시오."));
	su2 = parseInt(prompt("두 번째 수를 입력하시오."));
	suResult = "";
	while (true) {
		// 비교 - break	
		if (su1 == 0 && su2 == 0) break;
	
		// 비교 - su1 + su2 (100이상)
		if (su1 + su2 < 100) continue;
		suResult += su1 + su2; 
		// 출력 내용
		reStr = `${su1} 과 ${su2} 의 합은 ${suResult} 입니다.`;
		//출력 
		document.getElementById('result4').innerHTML = reStr;
	
	}
}
</script>
</head>
<body>
	<div class="box">
		<h3>사용자가 입력한 값을 계속 더하고,</h3>
		<h3>사용자가 0을 입력하면 그 때까지 누적된 값을 출력하라</h3>
		<input type="button" value="확인" onclick="proc1()"> 
		<br> <br>
		<div id="result1" class="result"></div>
	</div>
	
	<div class="box">
		<h3>다중 for 을 이용 i, k 를 더한다.</h3>
		<h3>조건 : 1 ~ 10 까지 더한 값이 3의 배수일 때만 출력하라! && continue를 이용하기!!</h3>
		<input type="button" value="확인" onclick="proc2()"> 
		<br> <br>
		<div id="result2" class="result"></div>
	</div>
	
	<div class="box">
		<h3>1~100 까지 중 2의 배수이면서 3의 배수인 것만 출력</h3>
		<input type="button" value="확인" onclick="proc3()"> 
		<br> <br>
		<div id="result3" class="result"></div>
	</div>

	<div class="box">
		<h3>두 수를 입력(prompt) 두 수의 합이 100 이상일 때만 출력</h3>
		<h3>조건: continue 를 이용, 두 수 모두 0이 입력되면 종료</h3>
		<input type="button" value="확인" onclick="proc4()"> 
		<br> <br>
		<div id="result4" class="result"></div>
	</div>
</body>
</html>