<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>String 주민번호</title>
<link rel ="stylesheet" href="../css/my.css">
<script>
	proc1 = () => {
		// 입력 
		ssn = prompt("주민등록번호를 입력하세요. 예 ) 111111-1111111");
		// 각 자리별로 자르기
		birYear = ssn.substr(0,2);
		birDay = `${ssn.substr(2,2)}월 ${ssn.substr(4,2)}일`;
		gender = ssn.substr(7,1);
		// 성별 비교 
		if (gender == 1 || gender == 3) genRes = `남자`;
		else genRes = `여자`; 
		// 년도 (2000 / 1900) 인지 비교 
		newBirYear ="";
		if (gender == 1 || gender == 2) {
			newBirYear == 19 + birYear;
		} else newBirYear == 20 + birYear;
		// 나이 계산 
		today = new Date();
		
		age = today.getFullYear() - newBirthYear;
		console.log(age);
		
		
		//출력 내용 (주민번호(999999-2*******), 생년월일, 성별, 나이)
	
	}
</script>
</head>
<body>
	<div class="box">
		<h3>
		prompt로 주민등록번호를 입력 받아 생년월일과 성별, 나이를 출 력하는 프로그램을 작성하시오.
		<br>• 예) 주민등록번호를 110326-4432618로 입력 받은 경우
		<br>• 생일 : 2011년 3월 26일 
		<br>• 성별:여자
		<br>• 나이:12
		</h3>

		<input type="button" onclick="proc1()" value ="확인">
		<div id="result1" class="result"></div>
	</div>

</body>
</html>