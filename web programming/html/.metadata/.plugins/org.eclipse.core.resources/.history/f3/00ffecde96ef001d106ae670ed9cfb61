<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
<link rel ="stylesheet" href="../css/my.css">
<script>
	// 랜덤 발생 (1~100)
	rand = parseInt(Math.random() * 100 + 1);
	console.log(rand);
	count = 0;
	//입력한 값들을 문자열로 누적시키는 변수 
	inputs = "";
	
	proc1 = () => {
		// 입력한 값 가져오기 - id="rnum"인 요소에 접근 - 값 가져오기 (value)
		numValue = document.getElementById('rnum').value;
		count++;
		inputs += numValue + " ";
		
		res="";
		
		// 비교 
		if (numValue == rand) {
			res = `랜덤 수 : ${rand} <br>`;
			res += `입력값들 : ${inputs} <br>`;
			res += `입력횟수 : ${count} <br> 정답입니다!`;
		} else if (numValue > rand){
			res = "힌트 : 더 작은 수를 입력하세요!!";
		} else {
			res = "힌트 : 더 큰 수를 입력하세요!!";
		}
		
		//출력 
		str = `${res}`;
		document.getElementById('result1').innerHTML = str;
	}
	
	arr = ["가위" , "바위" , "보"];
	
	proc2 = () => {
	com = arr[parseInt(Math.random()*3)];
	console.log(com);
	
	user = prompt("가위 바위 보를 입력하세요!");
	
	//비교
	if (com == user) {
		res = "비김!";
	} else if (user == "가위" && com == "보"
				|| user == "바위" && com == "가위"
				|| user == "보" && com == "바위") {
		res = "이김!";
	} else res = "짐!";
	
	//출력내용 
	str = `computer : ${com} <br>`;
	str += `user : ${user} <br>`;
	str += `결과 : ${res} <br>`;
	
	document.getElementById('result2').innerHTML = str;
</script>
</head>
<body>
	<div class="box">
		<h3>Random을 발생시켜 맞추는 프로그램 (1~100)</h3>
		<h3>힌트를 작성한다.</h3>
		
		<input type="text" id="rnum">
		<input type="button" onclick="proc1()" value ="확인">
		<div id="result1" class="result"></div>
	</div>

	<div class="box">
		<h3>가위바위보게임을할수있는프로그램을작성하시오. </h3>
		<h3>(컴퓨터는 랜덤, 사용자는 prompt로 입력 받아서 처리)</h3>
		
		<input type="button" onclick="proc2()" value ="확인">
		<div id="result2" class="result"></div>
	</div>

</body>
</html>